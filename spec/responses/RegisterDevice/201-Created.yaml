description: Created
content:
  application/json:
    schema:
      allOf:
        - $ref: ./../../schemas/ResponseSuccess.yaml
        - properties:
            body:
              type: object
              properties:
                endpointArn:
                  type: string
                  example: string
    example:
      $ref: ./201-Created.json
