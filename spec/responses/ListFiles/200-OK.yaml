description: OK
content:
  application/json:
    schema:
      allOf:
        - $ref: ./../../schemas/ResponseSuccess.yaml
        - properties:
            body:
              $ref: ./../../schemas/FileList.yaml
    example:
      $ref: ./200-OK.json
