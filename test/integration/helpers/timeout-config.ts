// Re-exported from @mantleframework/testing/integration
// Local implementations have been promoted to the framework.
export { getPollingConfig, getTimeout, isCI, logTimeoutConfig, POLLING, TIMEOUTS } from '@mantleframework/testing/integration'

export type { PollingConfig, TimeoutConfig } from '@mantleframework/testing/integration'
